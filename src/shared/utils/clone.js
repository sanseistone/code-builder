// ============================================================
// 深拷贝
// 两个工具原来各写了一份 JSON.parse(JSON.stringify(x))，收敛到此处
// ============================================================

/** 纯数据结构的深拷贝（配置对象均由 JSON 安全类型组成） */
export function clone(obj) {
  return JSON.parse(JSON.stringify(obj))
}
